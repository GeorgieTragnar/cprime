// Focused test: Footer exec - scope replacement only
exec <value> {
    -- Simple footer replacement test
    local val = params[0] or "DEFAULT"
    return "int footer_replaced = " .. val .. ";"
}<42>;

int main() {
    print("Testing footer replacement");
}