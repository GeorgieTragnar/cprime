// Test partial special members - this is now allowed in CPrime
class PartialClass {
    x: int,
    y: int,
    
    // Only define some special members - this is allowed in CPrime
    // All others are implicitly deleted
    PartialClass() = default;
    
    // Missing: copy constructor, move constructor, copy assignment, move assignment, destructor
    // These are all implicitly deleted unless explicitly declared
};

int main() {
    print("=== Partial Special Members Test ===");
    print("PartialClass defines only default constructor");
    print("All other special members are implicitly deleted");
    print("This demonstrates CPrime's explicit-only approach");
    print("=== Test Complete ===");
}