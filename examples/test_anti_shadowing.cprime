// Test anti-shadowing protection - this should trigger assertion

// Global exec alias
exec template<...> helper<T> {
    return "global helper";
};

namespace utils {
    // This should trigger anti-shadowing assertion!
    exec template<...> helper<T> {
        return "this shadows global helper - FORBIDDEN!";
    };
};

int main() {
    return 0;
}