// Binary Operator Expression Tests
// Tests: OPTIONAL_BINARY_OPERATOR pattern (arithmetic, comparison, logical)

// Arithmetic operations
int sum = 5 + 3;
int diff = 10 - 2;
int product = 4 * 6;
int quotient = 8 / 2;
int remainder = 9 % 4;

// Comparison operations
bool greater = 10 > 5;
bool less = 3 < 7;
bool equal = 5 == 5;
bool notEqual = 4 != 6;
bool greaterEqual = 8 >= 8;
bool lessEqual = 3 <= 9;

// Logical operations
bool andResult = true && false;
bool orResult = true || false;

// Mixed type arithmetic
float mixedResult = 3.14 + 2;
double precision = 1.5 * 2.0;